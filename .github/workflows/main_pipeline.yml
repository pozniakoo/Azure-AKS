name: '03 - Terraform main deployment'

on:
    push:
        branches:
            - main
        paths: 
            - 'Terraform/**'

permissions:
    contents: read

jobs:
    apply:
        runs-on: ubuntu-latest
        environment:
            name: Development
            url: ${{ steps.deploy.outputs.external_url }}

        steps:
            - name: 📦 Checkout Code
              uses: actions/checkout@v4

            - name: 🔑 Azure Login
              uses: azure/login@v1
              with:
                creds: ${{ secrets.AZURE_CREDENTIALS }}

            - name: 🛠️ Setup Terraform
              uses: hashicorp/setup-terraform@v3


            - name: 🔄 Terraform Init
              working-directory: ./Terraform
              run: terraform init

            - name: 🚀 Terraform Apply
              id: deploy
              working-directory: ./Terraform
              run: terraform apply -auto-approve